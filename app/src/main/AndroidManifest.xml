<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ninja.ninjadanny">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity -->
        <!-- android:name=".SingleInstanceActivity001" -->
        <!-- android:label="SingleInstanceActivity001"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="NINJA_DANNY" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity
            android:name=".splash.Splash_001"
            android:label="splash / Splash_001">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="NINJA_DANNY" />
            </intent-filter>
        </activity>

        <activity
            android:name=".splash.Splash_002"
            android:label="splash / Splash_002">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="NINJA_DANNY" />
            </intent-filter>
        </activity>

    </application>

</manifest>